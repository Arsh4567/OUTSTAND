{
  "buildCommand": "bun run build:vercel",
  "installCommand": "bun install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
