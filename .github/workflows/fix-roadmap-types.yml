name: Fix roadmap TypeScript types

on:
  push:
    branches: [fix/roadmap-types]

permissions:
  contents: write

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Triggering the main branch type-fix runner."
