name: Fix roadmap TypeScript types

on:
  push:
    branches: [fix/roadmap-types]

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Trigger
        run: echo "Temporary trigger; main runner performs the fix."
