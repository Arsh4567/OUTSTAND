import { motion } from "framer-motion";
import { Link } from "@tanstack/react-router";
import { ArrowRight, Brain, Check, Flame, Focus, Lightbulb, ListChecks, Sparkles, Target, Timer, Trophy, Zap } from "lucide-react";
import type { Habit, FocusSession, OutstandCompletion } from "@/lib/habits";
import { todayISO, computeStreak } from "@/lib/habits";
import type { DashboardMission } from "@/hooks/useDashboard";

const ease = [0.22, 1, 0.36, 1] as const;
const panel = "rounded-[28px] border border-white/[0.08] bg-white/[0.035] shadow-[0_24px_80px_-56px_rgba(34,211,238,.45)] backdrop-blur-xl";
function initialsColor(index: number) { const classes = ["border-cyan-300/15 bg-cyan-300/[0.07] text-cyan-200", "border-violet-300/15 bg-violet-300/[0.07] text-violet-200", "border-emerald-300/15 bg-emerald-300/[0.07] text-emerald-200", "border-amber-300/15 bg-amber-300/[0.07] text-amber-200"]; return classes[index % classes.length]; }

export function DashboardWelcome({ name, quote }: { name: string; quote: { quote: string; author: string } }) { return <motion.section initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .55, ease }} className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div className="max-w-3xl"><p className="text-[11px] font-black uppercase tracking-[0.28em] text-cyan-300/70">Welcome back</p><h1 className="mt-2 text-[2.35rem] font-black tracking-[-0.055em] text-white sm:text-5xl">{name}<span className="text-cyan-300">.</span></h1><div className="mt-4 flex max-w-2xl gap-3"><span className="mt-1 h-7 w-px shrink-0 bg-gradient-to-b from-cyan-300/70 to-transparent" /><div><p className="text-sm leading-6 text-slate-300">{quote.quote}</p><p className="mt-1 text-[10px] font-semibold uppercase tracking-[0.18em] text-slate-600">{quote.author}</p></div></div></div><div className="flex shrink-0 items-center gap-2 rounded-2xl border border-emerald-300/10 bg-emerald-300/[0.025] px-3 py-2 text-[10px] font-bold uppercase tracking-[0.16em] text-slate-500" title="Your dashboard is ready and up to date"><span className="h-2 w-2 rounded-full bg-emerald-400 shadow-[0_0_12px_rgba(52,211,153,.7)]" /> Ready for today</div></motion.section>; }

export function DashboardIntelligence({ habits, sessions, completedHabits, focusMinutes, bestStreak, nextMission }: { habits: Habit[]; sessions: FocusSession[]; completedHabits: number; focusMinutes: number; bestStreak: number; nextMission?: DashboardMission }) {
  const hasSystem = habits.length > 0; const today = todayISO(); const weakHabit = habits.find((habit) => !habit.history?.includes(today));
  const insight = !hasSystem ? "You haven't built your personal system yet. Start with a few habits that actually matter to you, and OUTSTAND can turn them into a daily rhythm." : completedHabits === habits.length ? "You have completed every habit you selected today. Your next advantage is protecting the streak with one focused block." : weakHabit ? `${weakHabit.name} is still open today. Completing it would close one of the gaps in your current routine.` : "Your routine is moving. The next useful step is a focused session, not another task on the list.";
  const action = !hasSystem ? "Choose your habits" : nextMission?.title || "Start a focused session"; const actionTo = !hasSystem ? "/habits" : "/focus";
  return <motion.section initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .08, duration: .5, ease }} className={`${panel} relative overflow-hidden p-5 sm:p-7`}><div className="pointer-events-none absolute -right-20 -top-24 h-64 w-64 rounded-full bg-cyan-400/[0.07] blur-3xl" /><div className="relative flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between"><div className="max-w-2xl"><div className="flex items-center gap-2 text-[10px] font-black uppercase tracking-[0.22em] text-cyan-300"><span className="grid h-7 w-7 place-items-center rounded-lg border border-cyan-300/15 bg-cyan-300/[0.07]"><Brain className="h-3.5 w-3.5" /></span> Your intelligence</div><h2 className="mt-4 text-xl font-black tracking-tight text-white sm:text-2xl">Here is what matters right now.</h2><p className="mt-2 text-sm leading-6 text-slate-400">{insight}</p></div><div className="grid min-w-0 gap-2 sm:grid-cols-3 lg:w-[410px]"><MiniSignal icon={<Flame />} label="Streak" value={`${bestStreak}d`} /><MiniSignal icon={<Timer />} label="Focused" value={`${focusMinutes}m`} /><MiniSignal icon={<ListChecks />} label="Today" value={`${completedHabits}/${habits.length || 0}`} /></div></div><div className="relative mt-6 flex flex-col gap-4 border-t border-white/[0.07] pt-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl border border-violet-300/15 bg-violet-300/[0.07] text-violet-200"><Lightbulb className="h-4 w-4" /></span><div><p className="text-[9px] font-black uppercase tracking-[0.18em] text-slate-600">Recommended next move</p><p className="mt-1 text-sm font-bold text-white">{action}</p></div></div><Link to={actionTo} className="inline-flex shrink-0 items-center justify-center gap-2 rounded-xl bg-cyan-300 px-4 py-2.5 text-[10px] font-black uppercase tracking-[0.14em] text-slate-950 shadow-[0_10px_30px_rgba(34,211,238,.16)] transition hover:-translate-y-0.5 hover:bg-cyan-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300/70">{hasSystem ? "Start now" : "Select habits"}<ArrowRight className="h-3.5 w-3.5" /></Link></div></motion.section>;
}
function MiniSignal({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="rounded-2xl border border-white/[0.07] bg-black/10 p-3"><div className="flex items-center gap-2 text-slate-500">{icon}<span className="text-[9px] font-black uppercase tracking-[0.16em]">{label}</span></div><p className="mt-2 text-xl font-black text-white">{value}</p></div>; }
export function DashboardMomentum({ xp, level, xpPct, streak, completedHabits, habitCount, focusMinutes }: { xp: number; level: number; xpPct: number; streak: number; completedHabits: number; habitCount: number; focusMinutes: number }) { return <section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><MomentumCard icon={<Zap />} label="Momentum" value={`${xp.toLocaleString()} XP`} detail={`Level ${level} · ${Math.round(xpPct)}% to next`} progress={xpPct} /><MomentumCard icon={<Flame />} label="Consistency" value={`${streak} day${streak === 1 ? "" : "s"}`} detail="Best active rhythm" /><MomentumCard icon={<Target />} label="Habits" value={`${completedHabits}/${habitCount}`} detail={habitCount ? "Completed today" : "Choose what matters"} progress={habitCount ? (completedHabits / habitCount) * 100 : 0} /><MomentumCard icon={<Focus />} label="Focus" value={`${focusMinutes} min`} detail="Completed sessions" /></section>; }
function MomentumCard({ icon, label, value, detail, progress }: { icon: React.ReactNode; label: string; value: string; detail: string; progress?: number }) { return <motion.div whileHover={{ y: -2 }} className="rounded-2xl border border-white/[0.07] bg-white/[0.025] p-4"><div className="flex items-center gap-2 text-slate-500"><span className="text-cyan-300">{icon}</span><span className="text-[9px] font-black uppercase tracking-[0.18em]">{label}</span></div><p className="mt-3 text-xl font-black tracking-tight text-white">{value}</p><p className="mt-1 text-[10px] font-medium text-slate-600">{detail}</p>{progress !== undefined && <div className="mt-3 h-1 overflow-hidden rounded-full bg-white/5"><motion.div initial={{ width: 0 }} animate={{ width: `${Math.min(100, Math.max(0, progress))}%` }} transition={{ duration: .8, ease }} className="h-full rounded-full bg-gradient-to-r from-cyan-400 to-violet-400" /></div>}</motion.div>; }
export function DashboardHabits({ habits, onToggle }: { habits: Habit[]; onToggle: (id: string) => void }) { const today = todayISO(); const completed = habits.filter((habit) => habit.history?.includes(today)).length; const visibleHabits = habits.slice(0, 4); const remaining = Math.max(0, habits.length - visibleHabits.length); return <motion.section initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .16, duration: .45, ease }} className={`${panel} relative overflow-hidden p-5 sm:p-6`}><div className="pointer-events-none absolute -right-16 -top-20 h-56 w-56 rounded-full bg-cyan-400/[0.06] blur-3xl" /><div className="relative flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[0.22em] text-cyan-300/70">Today’s habits</p><h2 className="mt-1 text-xl font-black tracking-tight text-white">Your routine, at a glance.</h2><p className="mt-1 text-sm text-slate-500">{habits.length ? `${completed} of ${habits.length} completed today` : "No habits selected yet"}</p></div><Link to="/habits" className="inline-flex items-center justify-center gap-2 rounded-xl border border-white/10 bg-white/[0.025] px-3.5 py-2.5 text-[10px] font-black uppercase tracking-[0.14em] text-slate-400 transition hover:border-cyan-300/20 hover:text-cyan-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300/60">Open habits <ArrowRight className="h-3.5 w-3.5" /></Link></div>{habits.length === 0 ? <Link to="/habits" className="relative mt-5 flex items-center gap-4 rounded-2xl border border-dashed border-cyan-300/15 bg-cyan-300/[0.025] p-4 transition hover:border-cyan-300/25 hover:bg-cyan-300/[0.045]"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl border border-cyan-300/15 bg-cyan-300/[0.07] text-cyan-200"><Sparkles className="h-4 w-4" /></span><span className="min-w-0 flex-1"><span className="block text-sm font-bold text-white">Build your habit set</span><span className="mt-1 block text-xs text-slate-500">Pick the habits you actually want to practice.</span></span><ArrowRight className="h-4 w-4 shrink-0 text-cyan-300" /></Link> : <div className="relative mt-5 grid gap-2 sm:grid-cols-2">{visibleHabits.map((habit, index) => { const done = habit.history?.includes(today); const streak = computeStreak(habit.history); return <button key={habit.id} type="button" onClick={() => onToggle(habit.id)} aria-pressed={done} aria-label={`${done ? "Unmark" : "Mark"} ${habit.name} ${done ? "as incomplete" : "as complete"}`} className={`group flex min-w-0 items-center gap-3 rounded-2xl border p-3 text-left transition ${done ? "border-emerald-300/15 bg-emerald-300/[0.045]" : "border-white/[0.07] bg-black/10 hover:border-cyan-300/15 hover:bg-white/[0.035]"}`}><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl border text-base ${initialsColor(index)}`}>{habit.emoji}</span><span className="min-w-0 flex-1"><span className={`block truncate text-sm font-bold ${done ? "text-emerald-200" : "text-white"}`}>{habit.name}</span><span className="mt-1 flex items-center gap-1.5 text-[9px] font-black uppercase tracking-[0.14em] text-slate-600">{done ? "Completed today" : streak > 0 ? `${streak} day streak` : "Open today"}</span></span><span className={`grid h-7 w-7 shrink-0 place-items-center rounded-lg border ${done ? "border-emerald-300/20 bg-emerald-300/10 text-emerald-300" : "border-white/10 text-slate-700 group-hover:text-cyan-300"}`}>{done ? <Check className="h-4 w-4" /> : <span className="h-1.5 w-1.5 rounded-full bg-current" />}</span></button>; })}{remaining > 0 && <Link to="/habits" className="sm:col-span-2 inline-flex items-center justify-center gap-2 rounded-xl border border-dashed border-white/[0.08] px-3 py-2.5 text-[10px] font-black uppercase tracking-[0.14em] text-slate-600 transition hover:border-cyan-300/15 hover:text-cyan-200">+{remaining} more habits <ArrowRight className="h-3 w-3" /></Link>}</div>}</motion.section>; }
export function DashboardRoadmap({ habits, missions, nextMission, onCompleteMission }: { habits: Habit[]; missions: DashboardMission[]; nextMission?: DashboardMission; onCompleteMission: (id: string) => void }) { const done = missions.filter((m) => m.completed).length; return <section className={`${panel} p-5 sm:p-6`}><div className="flex items-end justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[0.22em] text-slate-600">Your path today</p><h2 className="mt-1 text-xl font-black text-white">A few meaningful moves</h2></div><span className="text-xs font-bold text-cyan-300">{done}/{missions.length}</span></div><div className="mt-5 space-y-2">{missions.slice(0, 4).map((mission, index) => <div key={mission.id} className={`flex items-start gap-3 rounded-2xl border p-3 ${mission.completed ? "border-emerald-300/10 bg-emerald-300/[0.025]" : "border-white/[0.07] bg-black/10"}`}><button type="button" onClick={() => onCompleteMission(mission.id)} disabled={mission.completed || mission.mutating} aria-label={`${mission.completed ? "Completed" : "Complete"} mission: ${mission.title}`} className={`grid h-9 w-9 shrink-0 place-items-center rounded-xl border ${mission.completed ? "border-emerald-300/20 bg-emerald-300/10 text-emerald-300" : "border-white/10 text-slate-600 hover:border-cyan-300/20 hover:text-cyan-300"}`}>{mission.completed ? <Check className="h-4 w-4" /> : <span className="text-[10px] font-black">{index + 1}</span>}</button><div className="min-w-0 flex-1"><p className={`text-sm font-bold leading-5 ${mission.completed ? "text-slate-500 line-through" : "text-white"}`}>{mission.title}</p><p className="mt-1 text-[9px] font-black uppercase tracking-[0.15em] text-slate-600">{mission.category} · +{mission.xpReward} XP</p></div>{mission.mutating ? <span className="shrink-0 rounded-full bg-cyan-300/10 px-2 py-1 text-[9px] font-bold text-cyan-300">Saving</span> : mission.completed ? <span className="shrink-0 rounded-full bg-emerald-300/10 px-2 py-1 text-[9px] font-bold text-emerald-300">Done</span> : null}</div>)}</div><div className="mt-5 rounded-2xl border border-violet-300/10 bg-violet-300/[0.035] p-4"><div className="flex items-start gap-3"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-xl border border-violet-300/15 bg-violet-300/[0.07] text-violet-200"><Sparkles className="h-4 w-4" /></div><div><p className="text-[9px] font-black uppercase tracking-[0.16em] text-violet-200/70">Why this...</p><p className="mt-1 text-xs leading-5 text-slate-500">Missions are chosen around the systems you are actively building.</p></div></div></div></section>; }
export function DashboardActivity({ habits, sessions, outstand, dailyScore }: { habits: Habit[]; sessions: FocusSession[]; outstand: OutstandCompletion[]; dailyScore: number | null }) { const completedHabits = habits.reduce((sum, habit) => sum + (habit.history?.length ?? 0), 0); const completedSessions = sessions.filter((session) => session.completed).length; const completedOutstand = outstand.length; return <section className={`${panel} p-5 sm:p-6`}><div className="flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-[0.22em] text-slate-600">Activity</p><h2 className="mt-1 text-xl font-black text-white">Your recent output.</h2></div><Trophy className="h-5 w-5 text-amber-300/70" /></div><div className="mt-5 grid gap-2 sm:grid-cols-3"><MiniSignal icon={<Check />} label="Habit wins" value={`${completedHabits}`} /><MiniSignal icon={<Focus />} label="Focus blocks" value={`${completedSessions}`} /><MiniSignal icon={<Trophy />} label="Outputs" value={`${completedOutstand}`} /></div>{dailyScore !== null && <div className="mt-4 rounded-xl border border-white/[0.06] bg-black/10 p-3 text-xs text-slate-500">Latest daily score <span className="font-bold text-white">{dailyScore}</span></div>}</section>; }
