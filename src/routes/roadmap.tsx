import { createFileRoute } from "@tanstack/react-router";
import { ArrowRight, Pencil, Plus } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";
import { RoadmapOnboarding } from "@/components/roadmap/RoadmapOnboarding";
import { RoadmapVisualizer } from "@/components/roadmap/RoadmapVisualizer";
import { DailyFocusCard } from "@/components/roadmap/DailyFocusCard";
import { NightlyReviewModal } from "@/components/roadmap/NightlyReviewModal";
import { RoadmapEditDialog, type RoadmapEditPatch } from "@/components/roadmap/RoadmapEditDialog";
import { AdaptivePlanCard } from "@/components/roadmap/AdaptivePlanCard";
import { RecoveryModeCard } from "@/components/roadmap/RecoveryModeCard";
import { useRoadmap } from "@/hooks/use-roadmap";
import { useAdaptivePlanning } from "@/hooks/use-adaptive-planning";
import { useRecoveryIntelligence } from "@/hooks/use-recovery-intelligence";

export const Route = createFileRoute("/roadmap")({ component: RoadmapPage });
function daysBetweenTodayAnd(target: string) { const today = new Date(); const from = new Date(today.getFullYear(), today.getMonth(), today.getDate()); const to = new Date(`${target}T00:00:00`); return Math.max(0, Math.ceil((to.getTime() - from.getTime()) / 86400000)); }
function displayTitle(title: string, goal: string) { const clean = title.trim(); const target = clean.match(/\bto\s+(\d{3,4})\b/i)?.[1]; if (target) return `Road to ${target}`; const goalTarget = goal.match(/\b(?:to|reach|target)\s+(\d{3,4})\b/i)?.[1]; if (goalTarget) return `Road to ${goalTarget}`; return clean || "Your roadmap"; }
function cleanRoadmapName(title: string, goal: string) { const name = displayTitle(title, goal); return name.length > 32 ? `${name.slice(0, 32).trimEnd()}…` : name; }
function formatDaysLeft(days: number) { if (days <= 0) return "Target reached"; if (days === 1) return "1 day remaining"; return `${days} days remaining`; }
function RoadmapPage() {
  const roadmap = useRoadmap(); const [category, setCategory] = useState("skill_learning"); const [showOnboarding, setShowOnboarding] = useState(false); const [showRoadmaps, setShowRoadmaps] = useState(false); const [nightlyOpen, setNightlyOpen] = useState(false); const [reviewing, setReviewing] = useState(false); const [editOpen, setEditOpen] = useState(false); const [savingEdit, setSavingEdit] = useState(false); const [askingAI, setAskingAI] = useState(false); const [deleting, setDeleting] = useState(false);
  const startOnboarding = async () => { try { await roadmap.resetIntake(); await roadmap.askQuestions(category, {}); setShowOnboarding(true); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not start roadmap intake."); } }; const generate = async () => { try { const result = await roadmap.generate(category, roadmap.answers); if (result?.needsMoreInfo) { setShowOnboarding(true); return; } if (!result?.roadmapId) throw new Error("Roadmap was generated but no saved roadmap ID was returned."); setShowOnboarding(false); toast.success("Your roadmap is ready."); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not generate roadmap."); } }; const handleReview = async (reflection: string, energy: number, difficulty: number) => { setReviewing(true); try { const result = await roadmap.saveNightlyReview(reflection, energy, difficulty); toast.success(result?.reason || result?.analysis?.summary || "Tomorrow's priorities were adjusted."); setNightlyOpen(false); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not save nightly review."); } finally { setReviewing(false); } }; const saveEdit = async (patch: RoadmapEditPatch) => { const current = roadmap.roadmap; if (!current) return; setSavingEdit(true); try { await roadmap.updateRoadmap(current.id, patch); setEditOpen(false); toast.success("Roadmap updated."); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not update roadmap."); } finally { setSavingEdit(false); } }; const smartChange = async (request: string) => { const current = roadmap.roadmap; if (!current) return; setAskingAI(true); try { const result = await roadmap.smartChange(current.id, request); await roadmap.load(current.id); toast.success(result?.message || "Smart roadmap change applied."); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not apply smart change."); throw error; } finally { setAskingAI(false); } }; const deleteCurrentRoadmap = async () => { const current = roadmap.roadmap; if (!current) return; setDeleting(true); try { await roadmap.deleteRoadmap(current.id); setEditOpen(false); toast.success("Roadmap deleted."); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not delete roadmap."); throw error; } finally { setDeleting(false); } };
  const requiredTasks = useMemo(() => roadmap.tasks.filter((task) => task.is_required), [roadmap.tasks]); const completedCount = useMemo(() => requiredTasks.filter((task) => task.progress === "completed").length, [requiredTasks]); const todayRequired = roadmap.todayTasks.filter((task) => task.is_required); const todayCompleted = todayRequired.filter((task) => task.progress === "completed").length; const todayPercent = todayRequired.length ? Math.round((todayCompleted / todayRequired.length) * 100) : 0; const currentTitle = roadmap.roadmap ? displayTitle(roadmap.roadmap.title, roadmap.roadmap.goal) : "Your roadmap"; const daysLeft = roadmap.roadmap?.target_date ? daysBetweenTodayAnd(roadmap.roadmap.target_date) : Math.max(0, Number(roadmap.roadmap?.duration_days || 0) - roadmap.todayIndex); const plannedMinutesToday = todayRequired.reduce((sum, task) => sum + (Number(task.estimated_minutes) || 30), 0); const completedMinutesToday = todayRequired.filter((task) => task.progress === "completed").reduce((sum, task) => sum + (Number(task.estimated_minutes) || 30), 0); const adaptive = useAdaptivePlanning(roadmap.roadmap?.id, roadmap.roadmap ? { elapsedDays: roadmap.todayIndex, totalDays: Math.max(1, roadmap.roadmap.duration_days || 1), todayRequired: todayRequired.length, todayCompleted, totalRequired: requiredTasks.length, totalCompleted: completedCount, remainingDays: daysLeft, plannedMinutesToday, completedMinutesToday } : null); const recovery = useRecoveryIntelligence(roadmap.roadmap?.id, roadmap.roadmap ? { plannedTasks: todayRequired.length, completedTasks: todayCompleted, plannedMinutes: plannedMinutesToday, completedMinutes: completedMinutesToday, daysObserved: Math.max(1, roadmap.todayIndex), missedDays: 0, averageDifficulty: 3, recentDifficulty: 3 } : null); const applyRecovery = async () => { const ok = await recovery.applyRecovery(); if (ok) { toast.success("Recovery protocol saved. Keep the next day intentionally small."); await roadmap.load(roadmap.roadmap?.id); } else toast.error("Recovery protocol could not be saved."); }; const ambient = <div aria-hidden="true" className="pointer-events-none fixed inset-0 -z-10 overflow-hidden bg-[#050812]"><div className="absolute left-[4%] top-[5%] h-72 w-72 rounded-full bg-cyan-300/[.045] blur-3xl" /><div className="absolute right-[6%] top-[18%] h-80 w-80 rounded-full bg-violet-400/[.04] blur-3xl" /></div>;
  if (roadmap.loading) return <main className="relative min-h-screen overflow-hidden bg-[#050812] px-4 py-16 text-center text-slate-300">{ambient}<div className="mx-auto max-w-sm rounded-[2rem] border border-cyan-300/10 bg-[#07101d]/92 p-8"><div className="mx-auto h-12 w-12 animate-pulse rounded-2xl bg-gradient-to-br from-cyan-300/50 via-sky-400/35 to-violet-400/35" /><p className="mt-5 text-xs font-black uppercase tracking-[.18em]">Loading your roadmaps</p><p className="mt-2 text-sm text-slate-500">Restoring your saved plans and progress.</p></div></main>;
  if (showOnboarding || !roadmap.roadmap) return <main className="relative min-h-screen overflow-hidden bg-[#050812] px-4 py-8 text-slate-100 sm:px-6 lg:px-8">{ambient}<div className="relative mx-auto max-w-5xl py-8 sm:py-16"><div className="mx-auto max-w-3xl text-center"><div className="inline-flex rounded-full border border-cyan-300/20 bg-cyan-300/10 px-3 py-1 text-[10px] font-black uppercase tracking-[.18em] text-cyan-100">OUTSTAND / ROADMAP</div><h1 className="mt-6 text-4xl font-black tracking-[-.055em] sm:text-6xl">Choose the destination.<br /><span className="bg-gradient-to-r from-cyan-200 via-sky-300 to-violet-300 bg-clip-text text-transparent">We build the route.</span></h1><p className="mx-auto mt-4 max-w-2xl text-sm leading-6 text-slate-400 sm:text-base">A practical plan built from your goal, deadline, baseline, and real constraints.</p></div><section className="mx-auto mt-10 max-w-2xl rounded-[2rem] border border-cyan-300/10 bg-[#07101d]/92 p-6 sm:p-8">{roadmap.questions.length === 0 ? <><label className="text-xs font-black uppercase tracking-[.15em] text-slate-400">What are you trying to achieve?</label><select value={category} onChange={(e) => setCategory(e.target.value)} className="mt-3 w-full rounded-2xl border border-white/10 bg-[#050d18] px-4 py-3 text-sm font-bold text-slate-100 outline-none"><option value="skill_learning">Learn a skill</option><option value="academics">Academic goal</option><option value="exam_preparation">Exam preparation</option><option value="chess">Chess improvement</option><option value="fitness">Fitness goal</option><option value="content_creation">Content creation</option><option value="business">Business goal</option><option value="productivity">Productivity system</option></select><button type="button" onClick={() => void startOnboarding()} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-2xl bg-gradient-to-r from-cyan-300 via-sky-300 to-violet-300 px-5 py-3 text-sm font-black text-slate-950">Start intake <ArrowRight className="h-4 w-4" /></button></> : <RoadmapOnboarding category={category} questions={roadmap.questions} answers={roadmap.answers} onChange={roadmap.setAnswers} onNext={generate} generating={roadmap.generating} />}</section></div></main>;
  return <main className="relative min-h-screen overflow-hidden bg-[#050812] px-4 pb-24 pt-4 text-slate-100 sm:px-6 lg:px-8">{ambient}<div className="relative z-10 mx-auto max-w-7xl space-y-5 sm:space-y-7"><section className="rounded-[2rem] border border-white/10 bg-[#07101d]/92 p-5 shadow-[0_24px_80px_-55px_rgba(34,211,238,.25)] sm:p-7"><div className="flex flex-col gap-5 lg:flex-row lg:items-start lg:justify-between"><div className="min-w-0"><h1 className="text-3xl font-black tracking-tight sm:text-4xl">{currentTitle}</h1><div className="mt-4 flex flex-wrap items-center gap-2 text-[10px] font-black uppercase tracking-[.12em]"><span className="rounded-full border border-cyan-300/15 bg-cyan-300/[.05] px-3 py-1.5 text-cyan-100">{roadmap.roadmap.category || "goal"}</span><span className="rounded-full border border-white/10 bg-white/[.03] px-3 py-1.5 text-slate-500">Day {roadmap.todayIndex} of {roadmap.roadmap.duration_days}</span><span className="rounded-full border border-white/10 bg-white/[.03] px-3 py-1.5 text-slate-300">{formatDaysLeft(daysLeft)}</span></div></div><button type="button" onClick={() => setEditOpen(true)} className="inline-flex items-center gap-2 rounded-2xl border border-white/10 bg-white/[.03] px-4 py-3 text-xs font-black text-slate-200 hover:bg-white/[.06]"><Pencil className="h-4 w-4" /> Edit</button></div><div className="mt-5"><button type="button" aria-expanded={showRoadmaps} onClick={() => setShowRoadmaps((value) => !value)} className="group flex w-full items-center justify-between rounded-2xl border border-white/10 bg-white/[.025] px-4 py-3 text-left"><div><div className="text-[10px] font-black uppercase tracking-[.16em] text-slate-400">My roadmaps</div><div className="mt-1 text-xs text-slate-600">Choose a goal to continue.</div></div><div className="flex items-center gap-2"><span className="rounded-full border border-white/10 px-2.5 py-1 text-[10px] font-black uppercase tracking-[.12em] text-slate-500">{roadmap.roadmaps.length}/4</span><span aria-hidden="true" className={`text-lg text-slate-500 transition-transform ${showRoadmaps ? "rotate-180" : ""}`}>⌄</span></div></button>{showRoadmaps && <div className="mt-2 rounded-2xl border border-white/10 bg-[#050d18]/70 p-3"><div className="grid gap-2 sm:grid-cols-2">{roadmap.roadmaps.map((item, index) => <button key={item.id} type="button" title={displayTitle(item.title, item.goal)} onClick={() => void roadmap.selectRoadmap(item.id)} className={`rounded-2xl border px-3.5 py-3 text-left transition ${item.id === roadmap.roadmap?.id ? "border-cyan-300/30 bg-cyan-300/[.08]" : "border-white/10 bg-white/[.02] hover:border-white/15 hover:bg-white/[.05]"}`}><div className="flex items-center justify-between gap-2"><span className="text-[9px] font-black uppercase tracking-[.14em] text-slate-600">{index + 1}</span>{item.id === roadmap.roadmap?.id && <span className="h-1.5 w-1.5 rounded-full bg-cyan-200" />}</div><div className="mt-2 line-clamp-2 text-sm font-black leading-5 text-slate-200">{cleanRoadmapName(item.title, item.goal)}</div><div className="mt-1 text-[10px] font-semibold uppercase tracking-[.12em] text-slate-600">{item.category || "goal"}</div></button>)}<button type="button" disabled={roadmap.roadmaps.length >= 4} onClick={() => { roadmap.resetIntake(); roadmap.setAnswers({}); setCategory("skill_learning"); setShowOnboarding(true); }} className="rounded-2xl border border-dashed border-white/10 bg-white/[.01] px-3.5 py-3 text-left text-xs font-black text-slate-500"><Plus className="mb-2 h-4 w-4" /> Add roadmap</button></div></div>}</div></section><DailyFocusCard tasks={roadmap.todayTasks} onToggle={roadmap.toggleTask} /><RoadmapVisualizer milestones={roadmap.milestones} todayIndex={roadmap.todayIndex} />{adaptive.recommendation && <AdaptivePlanCard recommendation={adaptive.recommendation} latest={adaptive.latest} saving={adaptive.saving} onSave={adaptive.saveInsight} />}{recovery.recommendation && <RecoveryModeCard recommendation={recovery.recommendation} onApply={applyRecovery} applying={recovery.saving} />}<div className="flex flex-wrap gap-3"><button type="button" onClick={() => setNightlyOpen(true)} className="rounded-2xl border border-white/10 bg-white/[.03] px-4 py-3 text-xs font-black">Nightly review</button><button type="button" onClick={() => setEditOpen(true)} className="rounded-2xl border border-white/10 bg-white/[.03] px-4 py-3 text-xs font-black">Edit roadmap</button></div><RoadmapEditDialog open={editOpen} roadmap={roadmap.roadmap} onOpenChange={setEditOpen} onSave={saveEdit} onSmartChange={smartChange} onDelete={deleteCurrentRoadmap} saving={savingEdit || askingAI} deleting={deleting} /><NightlyReviewModal open={nightlyOpen} onOpenChange={setNightlyOpen} onSubmit={handleReview} submitting={reviewing} /></div></main>;
}
